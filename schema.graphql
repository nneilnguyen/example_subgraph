type Factory @entity {
  id: ID!
  poolCount: BigInt!
}

type Token @entity {
  id: ID!
  name: String!
  symbol: String!
  decimal: BigInt!
}

type Pool @entity {
  id: ID!
  token0: Token!
  token1: Token!
}
